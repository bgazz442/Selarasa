PORT=5000
NODE_ENV=development
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/selarasa?retryWrites=true&w=majority
JWT_SECRET=selarasa_super_secret_key_2026

# CORS Settings
# Set to true to allow all origins (for ngrok)
# In production, set to false and use CORS_ALLOWED_ORIGINS
CORS_ALLOW_ALL=true

# Comma-separated list of allowed origins (if CORS_ALLOW_ALL is false)
# Example: http://localhost:3000,https://your-ngrok-url.ngrok.io
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
